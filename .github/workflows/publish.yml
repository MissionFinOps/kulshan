name: Publish to PyPI

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/ci.yml
    with:
      # The full suite remains mandatory on branch and PR CI. Release publishing
      # uses the fast deterministic gates because hosted runners repeatedly
      # terminate the full-suite job before pytest can finish.
      run_full_tests: false

  publish:
    needs: quality
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # Required for trusted publishing
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

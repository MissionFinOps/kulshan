# Trust

**Status:** Not implemented — roadmap only

This module is a placeholder for trust verification features (IAM policy integrity checks,
write-action detection). No implementation exists beyond the IAM policy files at `Kulshan/iam/`.
This directory may be removed in a future cleanup.

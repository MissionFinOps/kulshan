[pytest]
testpaths = kulshan/tests
addopts = -q --strict-markers --cov=kulshan --cov-report=term-missing -p no:cacheprovider --basetemp=/tmp/kulshan-pytest
markers =
    slow: tests that take >5s
    integration: requires AWS mock setup